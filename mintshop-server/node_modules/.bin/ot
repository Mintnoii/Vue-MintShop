#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../opentype.js/bin/ot" "$@"
  ret=$?
else 
  node  "$basedir/../opentype.js/bin/ot" "$@"
  ret=$?
fi
exit $ret
